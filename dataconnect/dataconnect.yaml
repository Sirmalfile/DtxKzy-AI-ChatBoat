specVersion: "v1"
serviceId: "ai"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "ai-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
